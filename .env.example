# Provider selection (priority order):
#   1. ANTHROPIC_API_KEY set      -> Claude (claude-opus-4-8)
#   2. TUTOR_LOCAL_LLM_URL set    -> local Gemma 12B (OpenAI-compatible server)
#   3. neither                    -> built-in deterministic mock (zero config)

# 1) Real Claude. Leave blank to skip.
ANTHROPIC_API_KEY=

# 2) Local fallback — point at an Ollama / llama.cpp / LM Studio server.
#    Ollama example:
#      ollama pull gemma2:12b
#      TUTOR_LOCAL_LLM_URL=http://localhost:11434/v1
#      TUTOR_LOCAL_LLM_MODEL=gemma2:12b
TUTOR_LOCAL_LLM_URL=
TUTOR_LOCAL_LLM_MODEL=gemma2:12b
# Optional bearer token for servers that require one (LM Studio, vLLM, etc.).
TUTOR_LOCAL_LLM_KEY=

# Where documents (markdown + json + assets) are persisted.
TUTOR_DATA_DIR=./data

# API server port.
PORT=8787
